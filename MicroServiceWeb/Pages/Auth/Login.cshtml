@page
@model LibraryWeb.Pages.Auth.LoginModel
@{
    ViewData["Title"] = "Iniciar sesión";
}
<div class="auth-wrapper d-flex align-items-center justify-content-center py-5">
    <div class="auth-card card shadow-custom overflow-hidden" style="max-width: 920px;">
        <div class="row g-0">
            <div class="col-lg-5 d-none d-lg-flex bg-gradient-blue text-white align-items-center justify-content-center p-4">
                <div class="text-center">
                    <i class="fas fa-book-open" style="font-size:3rem;"></i>
                    <h2 class="mt-3 mb-1">Librería</h2>
                    <p class="mb-0 opacity-75">Sistema de Administración</p>
                </div>
            </div>
            <div class="col-lg-7 p-4 bg-white">
                <h5 class="mb-2 text-primary">Iniciar sesión</h5>
                <p class="text-muted mb-4">Accede con tu usuario o correo electrónico.</p>
                @if (!string.IsNullOrWhiteSpace(Model.ErrorMessage))
                {
                    <div class="alert alert-info" role="alert">@Model.ErrorMessage</div>
                }
                <form method="post" novalidate>
                    <div asp-validation-summary="ModelOnly" class="alert alert-danger py-2" role="alert"></div>
                    <div class="mb-3">
                        <label class="form-label text-muted small mb-1">Usuario o Correo</label>
                        <div class="input-group">
                            <span class="input-group-text bg-light text-primary"><i class="fas fa-user"></i></span>
                            <input asp-for="UserOrEmail" class="form-control" placeholder="Usuario o Correo" />
                        </div>
                        <span asp-validation-for="UserOrEmail" class="text-danger small"></span>
                    </div>
                    <div class="mb-3">
                        <label class="form-label text-muted small mb-1">Contraseña</label>
                        <div class="input-group">
                            <span class="input-group-text bg-light text-primary"><i class="fas fa-lock"></i></span>
                            <input asp-for="Password" class="form-control" type="password" placeholder="Contraseña" autocomplete="current-password" />
                        </div>
                        <span asp-validation-for="Password" class="text-danger small"></span>
                    </div>
                    <input type="hidden" asp-for="ReturnUrl" />
                    <button type="submit" class="btn btn-primary w-100">Entrar</button>
                </form>
            </div>
        </div>
    </div>
</div>
@section Scripts{ <partial name="Shared/_ValidationScriptsPartial" /> }
